// Accessing Modules
const profile = require('./others');

console.log(profile.getUser());
console.log(profile.getProfession());
console.log(profile.getMovie());
console.log(profile.ratingIMDB);

// run "node index.js" from terminal, which returns
// Tom Cruise
// Actor
// Mission Impossible
// 7.1